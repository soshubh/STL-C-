#include <bits/stdc++.h>
using namespace std;

int main() {
    multimap<int, int> mp;
    // everything same as map, only it can store wultiple keys
    // only map[key] can not be used here 
    
    return 0;
}
